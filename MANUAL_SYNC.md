# 手动多设备同步

小笺 1.2 的备份文件是设备无关的 JSON。导入时不会简单覆盖本机记录，而是按任务 ID 合并：同一任务保留 `updatedAt` 较新的版本，删除记录也会同步到另一台设备。

## 第一次同步

1. 在设备 A 打开“设置”，点击“导出备份”。
2. 使用公司允许的 U 盘、企业网盘或内部文件传输工具，把 JSON 文件带到设备 B。
3. 在设备 B 点击“导入并合并”，选择该文件。
4. 设备 B 会保留本机独有记录，同时加入设备 A 的记录。

## 双向同步

设备 B 使用一段时间后，再从 B 导出并导入到 A。可以反复执行，不会因为重复导入而重复创建任务。

```text
设备 A 导出 → 设备 B 合并
设备 B 导出 → 设备 A 合并
```

建议每次传输都使用最新导出的文件，并保留最近一份备份。不要手工编辑 JSON 中的任务 ID 和时间字段。

## 安全说明

备份文件目前是明文 JSON，适合放在自己控制的设备或公司批准的存储介质中。如果待办包含敏感信息，不要通过个人邮箱、公共网盘或未批准的聊天软件传输。后续可以增加密码加密备份。

## 手机端

当前 Electron 版本只支持 Windows 和 macOS，手机不能直接安装。备份格式已经可以被未来的手机端或 PWA 复用，但需要单独实现手机界面和本地存储适配。
